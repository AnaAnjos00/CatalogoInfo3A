import { Link } from "react-router-dom";
import "./globals.css";

export default function App() {
    return (
        
            const adicionarProdutoPedido = (produto) => {
             setPedidos([...listaPedidos, produto]);
}
        console.table(listaPedidos);

            const removerItem = (id) => {
                let remover = false;
                let listaAux = listaPedidos.filter((pedido)
                    {
                        if(pedido.id == id){

                        }
                    });
                    setPedidos(listaAux);
                }           


                  //Atividade 2: Criar estado para lista de produtos
                  //Atividade 3: Listar os objetos da lista de produtos
                  //Atividade 4: Criar estado e função para adicionar o objeto na lista de pedidos
                 //Atividade 5: Criar botão para adicionar o objeto na lista de pedidos
                  //Atividade 6: Listar os objetos da lista de pedidos
                  //Atividade 7: Criar a função para remover objeto da lista de pedidos
                 //Atividade 8: Criar botão para remover o objeto do pedido
               
        
    );
}
